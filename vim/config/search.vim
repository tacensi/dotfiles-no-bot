"" Search configurations

" Highlight the search item
set hlsearch

" Use incremental search (find as you type)
set incsearch

" Ignore case in searches
set ignorecase

"...unless a capital letter is included"
set smartcase

" turn off search hightlight
nnoremap <leader><space> :nohlsearch<CR>

